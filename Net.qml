pragma Singleton
import QtQuick
import Quickshell

// Placeholder created by the foundation wave so qmldir, shell.qml and every
// caller can be written once and never touched again. Owned and filled in by
// the Net track.
Singleton {
    id: root
}
